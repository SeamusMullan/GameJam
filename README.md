# Game Jam — Documentation

This repository contains docs for the Game Jam general plan. Use MkDocs to view and edit the documentation.

Local preview:

```bash
python -m pip install -r requirements.txt
mkdocs serve
```

See the `docs/` folder for the content.
