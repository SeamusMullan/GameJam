using UnityEngine;

public class TemplateScript : MonoBehaviour
{
    // Called after MonoBehaviour created, called before Update()
    void Start()
    {
        
    }

    // FixedUpdate is called every 'tick' (use this for physics / simulation / gameplay things)
    void FixedUpdate()
    {

    }

    // Update is called once per frame (use for UI, Scene Loading, Non-Gameplay things)
    void Update()
    {
        
    }
}
